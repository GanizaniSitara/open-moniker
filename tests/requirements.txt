# Test suite dependencies for Open Moniker
# Install with: pip install -r tests/requirements.txt

# HTTP client for async requests
aiohttp>=3.9.0

# HTTP client for synchronous requests
requests>=2.31.0

# Optional: for better performance testing
# uvloop>=0.19.0  # Linux/macOS only, faster event loop
